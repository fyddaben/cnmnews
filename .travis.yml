language: node_js
node_js:
  - '4.7.0'
script:
  - sh ./run.sh prod
install:
  - npm install -g firebase-tools
after_success:
  - firebase deploy --token $FIREBASE_TOKEN
branches:
  only:
      - master
